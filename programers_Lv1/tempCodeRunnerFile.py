for i in skip:
    #     s_list.remove(i)

    # new_s = "".join(s_list)

    # for i in s:
    #     idx = new_s.find(i) + index
    #     if idx >= len(new_s):
    #         idx = idx - len(new_s)
    #     answer += new_s[idx]
    # return answer
